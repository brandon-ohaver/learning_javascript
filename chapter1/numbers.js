// call numbers in the following ways

// whole number
12

// scientific notation
2.998e8

// decimal
3.14

// using arithmetic on numbers
100 + 4 * 11 // this will perform the multiplication then addition

(100 + 4) * 11 // this will force the addition first

// arithmetic in JS is solved with precedence
100 * 12 / 3 // both the (*) and (/) operators hold the same precedence so the arithmetic is solved left-to-right





